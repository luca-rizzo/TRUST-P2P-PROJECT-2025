import { FormatBase18Pipe } from './format-base18.pipe';

describe('FormatBase18Pipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBase18Pipe();
    expect(pipe).toBeTruthy();
  });
});
